import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 10,
  },
  headLine: {
    fontSize: 18,
    fontWeight: '700',
  },
});

export default styles;
